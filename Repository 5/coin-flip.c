#include <stdio.h>
int main (){
    srand(time(NULL));
    scanf("%\,& coinFlip");
    int coinFlip = (rand() % 2) + 1;
     if( coinflip == 1){
         printf("is 1 == tails")
     }

    return 0;
}